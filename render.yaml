services:
  - type: web
    name: icu-ai-manual
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
